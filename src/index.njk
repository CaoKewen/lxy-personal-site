---
layout: layouts/base.njk
title: Home
---
<section>
  <h2>Welcome to LXY's Learning Journal</h2>
  <p>This site records learning notes, study reflections, and project summaries.</p>

  <h3>Recent Posts</h3>
  {% set postslist = collections.posts %}
  {% if postslist and postslist.length > 0 %}
    <ul>
      {% for post in postslist | reverse | slice(0, 5) %}
        <li>
          <a href="{{ post.url }}">{{ post.data.title }}</a>
          <small> — {{ post.date | readableDate }}</small>
        </li>
      {% endfor %}
    </ul>
  {% else %}
    <p>No posts yet — write the first one!</p>
  {% endif %}
</section>
