---
layout: layouts/base.njk
title: All Posts
permalink: /posts/
---

<section>
  <h2>📚 All Posts</h2>
  <p style="color: var(--muted); margin-bottom: 2rem;">
    Browse my learning journal organized by category: homework solutions, paper notes, insights, and spontaneous ideas.
  </p>

  <!-- Homework -->
  <div style="margin-bottom: 3rem;">
    <h3 style="color: var(--accent); display: flex; align-items: center; gap: 0.5rem;">
      📝 Homework
    </h3>
    <p style="color: var(--muted); font-size: 0.95rem; margin-bottom: 1rem;">
      Problem sets and exercise solutions
    </p>
    {% if collections.homework and collections.homework.length > 0 %}
      <ul style="list-style: none; margin-left: 0; padding: 0;">
        {% for post in collections.homework %}
          <li style="padding: 0.75rem 0; border-bottom: 1px solid var(--border);">
            <a href="{{ post.url }}" style="font-size: 1.05rem; font-weight: 500;">{{ post.data.title }}</a>
            <small style="color: var(--muted); margin-left: 0.5rem;">{{ post.date | readableDate }}</small>
          </li>
        {% endfor %}
      </ul>
    {% else %}
      <p style="color: var(--muted); font-style: italic;">No homework posts yet.</p>
    {% endif %}
  </div>

  <!-- Paper Reading -->
  <div style="margin-bottom: 3rem;">
    <h3 style="color: var(--accent); display: flex; align-items: center; gap: 0.5rem;">
      📄 Paper Reading
    </h3>
    <p style="color: var(--muted); font-size: 0.95rem; margin-bottom: 1rem;">
      Notes and summaries from research papers
    </p>
    {% if collections.paperReading and collections.paperReading.length > 0 %}
      <ul style="list-style: none; margin-left: 0; padding: 0;">
        {% for post in collections.paperReading %}
          <li style="padding: 0.75rem 0; border-bottom: 1px solid var(--border);">
            <a href="{{ post.url }}" style="font-size: 1.05rem; font-weight: 500;">{{ post.data.title }}</a>
            <small style="color: var(--muted); margin-left: 0.5rem;">{{ post.date | readableDate }}</small>
          </li>
        {% endfor %}
      </ul>
    {% else %}
      <p style="color: var(--muted); font-style: italic;">No paper reading posts yet.</p>
    {% endif %}
  </div>

  <!-- Insights -->
  <div style="margin-bottom: 3rem;">
    <h3 style="color: var(--accent); display: flex; align-items: center; gap: 0.5rem;">
      💡 Insights
    </h3>
    <p style="color: var(--muted); font-size: 0.95rem; margin-bottom: 1rem;">
      Clarifying thoughts and consolidating understanding
    </p>
    {% if collections.insights and collections.insights.length > 0 %}
      <ul style="list-style: none; margin-left: 0; padding: 0;">
        {% for post in collections.insights %}
          <li style="padding: 0.75rem 0; border-bottom: 1px solid var(--border);">
            <a href="{{ post.url }}" style="font-size: 1.05rem; font-weight: 500;">{{ post.data.title }}</a>
            <small style="color: var(--muted); margin-left: 0.5rem;">{{ post.date | readableDate }}</small>
          </li>
        {% endfor %}
      </ul>
    {% else %}
      <p style="color: var(--muted); font-style: italic;">No insights posts yet.</p>
    {% endif %}
  </div>

  <!-- Musings -->
  <div style="margin-bottom: 3rem;">
    <h3 style="color: var(--accent); display: flex; align-items: center; gap: 0.5rem;">
      ✨ Musings
    </h3>
    <p style="color: var(--muted); font-size: 0.95rem; margin-bottom: 1rem;">
      A collection of spontaneous ideas, unfinished questions, and random sparks of curiosity
    </p>
    {% if collections.musings and collections.musings.length > 0 %}
      <ul style="list-style: none; margin-left: 0; padding: 0;">
        {% for post in collections.musings %}
          <li style="padding: 0.75rem 0; border-bottom: 1px solid var(--border);">
            <a href="{{ post.url }}" style="font-size: 1.05rem; font-weight: 500;">{{ post.data.title }}</a>
            <small style="color: var(--muted); margin-left: 0.5rem;">{{ post.date | readableDate }}</small>
          </li>
        {% endfor %}
      </ul>
    {% else %}
      <p style="color: var(--muted); font-style: italic;">No musings posts yet.</p>
    {% endif %}
  </div>

</section>
